g++ -c AVL.cpp ***.cpp 可以生成 .o文件  
ar -r ***(自定义静态库名) ***.o  由 .o文件生成静态库  可以将AVL.cpp中定义的函数加入到静态库
g++ ***.cpp(主函数，调用了AVL.cpp中定义的函数)  ***(包含所调用函数的静态库名) -o ***(自定义可执行文件名)

也可直接 g++ ***.cpp(含主函数程序文件) AVL.cpp(主函数调用的函数的定义文件) -o ***(自定义可执行文件名)